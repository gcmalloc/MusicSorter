
class MusicFileCluster():
    
    def __init__(self, musicFile*):
        self.musicFiles = set()
        for i in musicFile:
            self.musicFiles.add()

    def weight_files():
        #files are together so they should look a bit the same
        
